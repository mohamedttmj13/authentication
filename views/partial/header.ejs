<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%if(homePageTitle){%>
      <title>Authentication-App | Home</title>
    <%}%>
    <%if(signUpPageTitle){%>
      <title>Authentication-App | Sign Up</title>
    <%}%>
    <%if(signInPageTitle){%>
      <title>Authentication-App | Sign In</title>
    <%}%>
    <%if(resetPageTitle){%>
      <title>Authentication-App | Reset password</title>
    <%}%>
    <%if(changePageTitle){%>
      <title>Authentication-App | Change password</title>
    <%}%>

    <meta name="theme-color" content="#7952b3">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="css/app.css">
</head>
<body class="text-center">

    <div class="container">
        <header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
          <a href="/" class="d-flex align-items-center col-md-3 mb-2 mb-md-0 text-decoration-none app-brand">
            Authentication <span >app</span>
          </a>
    
          <ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
            <li><a href="/" class="nav-link px-2 link-secondary <%= homeNavItem %>">Home</a></li>
             <% if(isAuthenticated){ %>
              <li><a href="/change" class="nav-link px-2 link-secondary <%= changepassNavItem %>">Change password</a></li>
             <%}%>
          </ul>
    
          <div class="col-md-3 text-end">
            <% if(isAuthenticated){ %>
                <a type="button" href="/singOut" class="btn btn-outline-primary me-2 sign">Sign out</a>
            <% }else{ %>
              <a type="button" href="/login" class="btn btn-outline-primary me-2 sign">Sign in</a>
            <a type="button" href="/register" class="btn btn-primary signup">Sign up</a>
            <% } %>
          </div>
        </header>